#define Esc   0x1b
#define Enter 0x0d

#ifndef MAX
#define MAX(x,y) ((x) > (y) ? (x) : (y))
#endif
#ifndef MIN
#define MIN(x,y) ((x) < (y) ? (x) : (y))
#endif

#define pi 3.141592653589793238462643
#define eps 1.0e-6
#define N 32
#define radius 16
#define Mxz 33

#define NTAB 240

#define Block_Sizez 16
#define Block_Sizex 16


